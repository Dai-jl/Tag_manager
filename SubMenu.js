export class SubMenu{
    constructor(title,icon,section){
        this.title = title ? title : '';
        this.icon = icon ? icon : '';
        this.section = section ? section : '';//TODO
    }
    onHover(){
        
    }
}